# Быстрый старт - Interior Studio CRM (многопользовательский режим)

## Что уже сделано

✅ Сервер развернут на Timeweb Cloud VPS
✅ API доступен по адресу: **https://147.45.154.193**
✅ База данных PostgreSQL настроена
✅ Создан администратор: `admin` / `admin123`
✅ Клиент PyQt5 настроен для работы с API

---

## Проверка работы (прямо сейчас!)

### 1. Проверить сервер

Откройте браузер: https://147.45.154.193

Должны увидеть:
```json
{
  "app": "Interior Studio CRM API",
  "version": "1.0.0",
  "status": "running"
}
```

### 2. Запустить клиент

```bash
# Из папки d:\New CRM\interior_studio\
.venv\Scripts\python.exe main.py
```

### 3. Войти в систему

- **Логин:** admin
- **Пароль:** admin123

При успешном входе в логах увидите:
```
[INFO] API клиент инициализирован: https://147.45.154.193
[INFO] API аутентификация: логин='admin'
[INFO] Успешный API вход: Администратор (роль: Руководитель студии)
```

---

## Режимы работы

### Многопользовательский (текущий):
```python
# config.py
MULTI_USER_MODE = True
API_BASE_URL = "https://147.45.154.193"
```

### Локальный (для тестирования):
```python
# config.py
MULTI_USER_MODE = False
```

---

## Что делать дальше

### Вариант 1: Протестировать вход через API
Запустите приложение и войдите как admin - это проверит всю цепочку.

### Вариант 2: Создать дополнительных пользователей
```bash
ssh root@147.45.154.193
cd /opt/interior_studio
docker-compose exec postgres psql -U crm_user -d interior_studio_crm
```

```sql
-- Создать менеджера
INSERT INTO employees (
    full_name, login, password_hash,
    position, role, status,
    created_at, updated_at
) VALUES (
    'Менеджер Тестовый',
    'manager',
    '$2b$12$LQv3c1yqBWVHxkd0LHAkCOYz6TtxMQJqhN8/LewY5oDgbHQJO9EGu',  -- admin123
    'Менеджер',
    'Менеджер',
    'активный',
    NOW(),
    NOW()
);
```

### Вариант 3: Развернуть на других компьютерах
1. Скопировать папку `interior_studio`
2. Убедиться что в `config.py` установлено:
   ```python
   MULTI_USER_MODE = True
   API_BASE_URL = "https://147.45.154.193"
   ```
3. Запустить и войти

---

## Файлы документации

- **CLIENT_SETUP.md** - полная документация клиента
- **TIMEWEB_DEPLOY.md** - развертывание сервера
- **MULTI_USER_SETUP.md** - архитектура API
- **server/README.md** - API endpoints

---

## Проблемы?

### Не подключается к серверу:
```bash
# Проверить сервер
ping 147.45.154.193
curl https://147.45.154.193
```

### Неверный логин/пароль:
- Проверьте что пишете `admin` (маленькими буквами)
- Пароль: `admin123`

### Переключиться на локальный режим:
```python
# config.py
MULTI_USER_MODE = False  # Временно
```

---

## Управление сервером

```bash
# SSH подключение
ssh root@147.45.154.193

# Логи API
cd /opt/interior_studio
docker-compose logs -f api

# Перезапуск
docker-compose restart api

# Статус
docker-compose ps
```

---

**Готово к работе!** 🚀

Следующие шаги см. в CLIENT_SETUP.md (синхронизация, обновление вкладок и т.д.)
