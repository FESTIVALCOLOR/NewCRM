# Инструкция по настройке обновлений через Яндекс Диск

## ✅ Что уже готово:

1. ✅ Система версионирования реализована
2. ✅ UpdateManager создан
3. ✅ Интерфейс обновлён (версия в статус-баре + кнопка)
4. ✅ **EXE файл собран:** `d:\New CRM\interior_studio\dist\InteriorStudio.exe` (59 МБ)

---

## 📝 Что нужно сделать:

### Шаг 1: Создать папку на Яндекс Диске

1. Зайдите на https://disk.yandex.ru
2. Создайте папку **`CRM_UPDATES`**

### Шаг 2: Создать структуру внутри CRM_UPDATES

Создайте следующую структуру:

```
CRM_UPDATES/
├── version.json        ← ФАЙЛ (создайте текстовый файл)
└── v1.0.0/             ← ПАПКА (создайте папку)
    └── InteriorStudio.exe  ← ФАЙЛ (загрузите собранный exe)
```

### Шаг 3: Загрузить exe файл

1. Откройте папку `d:\New CRM\interior_studio\dist\`
2. Найдите файл `InteriorStudio.exe` (размер ~59 МБ)
3. Загрузите его на Яндекс Диск в папку `CRM_UPDATES/v1.0.0/`

### Шаг 4: Сделать файл публичным

1. На Яндекс Диске кликните правой кнопкой на файл `InteriorStudio.exe`
2. Выберите **"Поделиться"** → **"Получить ссылку"**
3. Скопируйте ссылку (будет вида `https://disk.yandex.ru/d/КЛЮЧ`)

### Шаг 5: Получить прямую ссылку для скачивания

**Способ 1: Простой (добавить параметр)**
- Ваша ссылка: `https://disk.yandex.ru/d/ABC123`
- Прямая ссылка: `https://disk.yandex.ru/d/ABC123?force_download=1`

**Способ 2: Через API (надёжнее)**
1. Откройте в браузере:
   ```
   https://cloud-api.yandex.net/v1/disk/public/resources/download?public_key=https://disk.yandex.ru/d/ВАШ_КЛЮЧ
   ```
2. В ответе будет JSON, найдите поле `"href"` - это прямая ссылка

### Шаг 6: Создать version.json

Создайте файл `version.json` в папке `CRM_UPDATES` со следующим содержимым:

```json
{
  "latest_version": "1.0.0",
  "versions": {
    "1.0.0": {
      "release_date": "2025-12-21",
      "download_url": "ВСТАВЬТЕ_ПРЯМУЮ_ССЫЛКУ_НА_EXE",
      "changelog": "Первая версия с системой обновлений",
      "size_mb": 59,
      "required": false
    }
  }
}
```

**Замените** `"ВСТАВЬТЕ_ПРЯМУЮ_ССЫЛКУ_НА_EXE"` на прямую ссылку из Шага 5.

### Шаг 7: Сделать папку CRM_UPDATES публичной

1. Кликните правой кнопкой на папку **`CRM_UPDATES`**
2. Выберите **"Поделиться"** → **"Получить ссылку"**
3. Скопируйте публичную ссылку (например: `https://disk.yandex.ru/d/XYZ789`)
4. Извлеките **публичный ключ** из ссылки:
   - Ссылка: `https://disk.yandex.ru/d/XYZ789`
   - Ключ: `XYZ789`

### Шаг 8: Настроить config.py

Откройте файл `d:\New CRM\interior_studio\config.py` и найдите строку:

```python
UPDATE_YANDEX_PUBLIC_KEY = ""  # Публичный ключ папки с обновлениями
```

Вставьте ваш публичный ключ:

```python
UPDATE_YANDEX_PUBLIC_KEY = "XYZ789"  # Замените на ваш ключ
```

---

## 🧪 Тестирование

После настройки:

1. Запустите программу: `python main.py`
2. Войдите как **"Руководитель студии"** (admin)
3. В правом нижнем углу увидите: **"Версия: 1.0.0"** и кнопку обновления
4. Нажмите на **кнопку обновления** (иконка refresh)
5. Должно показать: **"У вас установлена последняя версия программы"**

### Дополнительные тесты:

- **Shift + клик** по кнопке обновления → откроется диалог изменения версии
- Измените версию на `1.0.1` → перезапустите программу → версия изменится

---

## 🚀 Выпуск новой версии (в будущем)

Когда захотите выпустить версию 1.1.0:

1. Измените версию в `config.py`:
   ```python
   APP_VERSION = "1.1.0"
   ```

2. Соберите новый exe:
   ```bash
   .venv\Scripts\pyinstaller.exe InteriorStudio.spec --clean --noconfirm
   ```

3. На Яндекс Диске:
   - Создайте папку `CRM_UPDATES/v1.1.0/`
   - Загрузите новый `InteriorStudio.exe`
   - Сделайте файл публичным
   - Получите прямую ссылку

4. Обновите `version.json`:
   ```json
   {
     "latest_version": "1.1.0",
     "versions": {
       "1.1.0": {
         "release_date": "2025-12-22",
         "download_url": "НОВАЯ_ПРЯМАЯ_ССЫЛКА",
         "changelog": "Добавлена облачная синхронизация",
         "size_mb": 60,
         "required": false
       },
       "1.0.0": {
         "release_date": "2025-12-21",
         "download_url": "СТАРАЯ_ССЫЛКА",
         "changelog": "Первая версия с системой обновлений",
         "size_mb": 59,
         "required": false
       }
     }
   }
   ```

5. Пользователи с версией 1.0.0 увидят уведомление об обновлении!

---

## ❓ Частые вопросы

**Q: Что делать, если забыл публичный ключ?**
A: Зайдите на Яндекс Диск, кликните на папку CRM_UPDATES правой кнопкой → "Поделиться" → ссылка будет там.

**Q: Можно ли использовать другое облако?**
A: Да, но нужно будет переписать UpdateManager для работы с Google Drive, Dropbox и т.д.

**Q: База данных обновляется вместе с программой?**
A: НЕТ! БД остаётся отдельно и не затрагивается при обновлении exe файла.

---

## 📞 Если нужна помощь

Скажите на каком шаге возникли проблемы, и я помогу!
