# Фичи и спецификации

> Бизнес-логика, Kanban, платежи, таймлайны, надзор, Яндекс.Диск, offline-режим.
> Последнее обновление: 2026-03-29

## Двухрежимная архитектура

### Сетевой режим (Data Flow)

```
LoginWindow → APIClient.authenticate() → (JWT токен + employee_data)
    → MainWindow + SyncManager (QTimer 30 сек)
    → Табы работают через DataAccess (API + fallback на SQLite)
    → При изменении: DataAccess → API + offline очередь
```

### Автономный режим (Data Flow)

```
LoginWindow → DatabaseManager.verify_credentials() (SQLite)
    → MainWindow (без SyncManager)
    → Табы работают через DataAccess (только SQLite)
```

## Kanban CRM доска

### Индивидуальные проекты (6 колонок)

```
1. Новый заказ
2. В ожидании
3. Стадия 1: планировочные решения
4. Стадия 2: концепция дизайна
5. Стадия 3: рабочие чертежи
6. Выполненный проект
```

### Шаблонные проекты (5 колонок)

```
1. Новый заказ
2. В ожидании
3. Стадия 1: планировочные решения
4. Стадия 2: рабочие чертежи
5. Выполненный проект
```

### Типы проектов

| Тип | Подтипы |
|-----|---------|
| Индивидуальный | Полный (с 3D), Эскизный (с коллажами), Планировочный |
| Шаблонный | Стандарт, Стандарт с визуализацией, Проект ванной, Проект ванной с визуализацией |

### Drag & Drop

При перемещении карточки:
1. Обновляется `column_name`
2. Пересчитывается `order_position`
3. Валидация перемещения (нельзя назад, проверка аванса)
4. Оптимистичное обновление UI с откатом при ошибке

## Workflow — рабочий процесс стадии

```
Назначение исполнителя (ExecutorSelectionDialog)
  → deadline = assigned_date + norm_days рабочих дней
  → stage_executor.completed = False
          │
          ▼
Исполнитель: "Сдать работу"
  → API: complete_stage_for_executor() + workflow_submit()
  → stage_executor.completed = True
  → Кнопка → "Ожидайте проверку" (disabled)
          │
          ▼
Менеджер: "Принять работу" / "На исправление"
  ├── "Принять" → workflow_accept() → дата в timeline
  └── "На исправление" → workflow_reject()
        → stage_executor.completed = False
        → Загрузка файла правок (опционально)
        → Исполнитель снова видит "Сдать работу"
          │
          ▼
"Клиенту на согласование" → workflow/client-send
  → "Клиент согласовал" → workflow/client-ok (идемпотентный)
```

### DB модель StageWorkflowState

```sql
status: 'in_progress' | 'revision' | 'client_approval' | 'completed'
revision_count: Integer   -- кол-во исправлений
```

### Диагностика карточки

Кнопка «Диагностика» (иконка гаечного ключа):
- Проверяет соответствие состояния реальному таймлайну
- Автоматически восстанавливает карточку при «зависании»
- Доступна: руководитель, старший менеджер, СДП, ГАП

## Таблица сроков (Project Timeline)

### 7 колонок

| # | Колонка | Тип |
|---|---------|-----|
| 1 | Действия по этапам | Текст (название стадии) |
| 2 | Дата | QDateEdit (yyyy-MM-dd) |
| 3 | Кол-во дней | Integer (actual_days) |
| 4 | Норма дней | Integer (norm_days) |
| 5 | Статус | Текст |
| 6 | Исполнитель | Роль (Дизайнер/Чертёжник/...) |
| 7 | ФИО | Имя назначенного сотрудника |

### Расчёт norm_days

```python
K = max(0, int((area - 1) // 100))  # коэффициент площади
raw_norm_days = базовые_дни(K)       # для каждого этапа
norm_days = contract_term * (raw_norm_days / sum(in_scope_raw))  # пропорционально
```

### Расчёт actual_days

```python
actual_days = networkdays(prev_date, current_date)  # рабочие дни (Пн-Пт)
```

### Авто-расчёт START

```python
START = max(contract_date, survey_date, tech_task_date)
```

### Шаблон индивидуальных проектов (stage codes: S*)

| Группа | Этапы | Полный | Эскизный | Планировочный |
|--------|-------|--------|----------|---------------|
| START | Дата начала | Да | Да | Да |
| STAGE1 | Планировочные решения (3 подэтапа) | Да | Да | Да |
| STAGE2 | Концепция дизайна (7 подэтапов) | Да | Частично | Нет |
| STAGE3 | Рабочая документация (12 шагов) | Да | Да | Нет |

### Шаблон шаблонных проектов (stage codes: T*)

| Группа | Этапы | Стандарт | С визуализацией |
|--------|-------|----------|----------------|
| START | Дата начала | Да | Да |
| T1 | Планировочные решения | Да | Да |
| T2 | Рабочие чертежи | Да | Да |
| T3 | 3D визуализация | Нет | Да |

## Авторский надзор

### Канбан надзора

Карточки надзора на Kanban-доске:
- Пауза/возобновление с указанием причины
- Перемещение через long-press диалог
- Связь с договором (contract_id)

### Диалог карточки надзора (вкладки)

1. **Информация** — данные договора, блок «Команда»
2. **Таблица закупок** — 12 стадий, бюджет, статусы
3. **Выезды** — таблица выездов и дефектов (новая вкладка)
4. **История** — события, голосовые заметки
5. **Чат** — Telegram чат проекта
6. **Файлы** — документы на ЯД

### Таблица закупок надзора

**11 колонок:**

| # | Колонка | Тип |
|---|---------|-----|
| 1 | Стадия | Текст |
| 2 | План. дата | Date |
| 3 | Факт. дата | Date |
| 4 | Дней | Integer |
| 5 | Бюджет план | Float (руб) |
| 6 | Бюджет факт | Float (руб) |
| 7 | Экономия | Float (план - факт) |
| 8 | Поставщик | Текст |
| 9 | Комиссия | Float (%) |
| 10 | Статус | Не начато/В работе/Закуплено/Доставлено/Просрочено |
| 11 | Примечания | Text |

**12 стандартных стадий закупок:**

| Код | Стадия |
|-----|--------|
| STAGE_1_CERAMIC | Стадия 1: Закупка керамогранита |
| STAGE_2_PLUMBING | Стадия 2: Закупка сантехники |
| STAGE_3_EQUIPMENT | Стадия 3: Закупка оборудования |
| STAGE_4_DOORS | Стадия 4: Закупка дверей и окон |
| STAGE_5_WALL | Стадия 5: Закупка настенных материалов |
| STAGE_6_FLOOR | Стадия 6: Закупка напольных материалов |
| STAGE_7_STUCCO | Стадия 7: Лепной декор |
| STAGE_8_LIGHTING | Стадия 8: Освещение |
| STAGE_9_APPLIANCES | Стадия 9: Бытовая техника |
| STAGE_10_CUSTOM_FURNITURE | Стадия 10: Закупка заказной мебели |
| STAGE_11_FACTORY_FURNITURE | Стадия 11: Закупка фабричной мебели |
| STAGE_12_DECOR | Стадия 12: Закупка декора |

### Выезды надзора (SupervisionVisits)

**Поля записи выезда:**

| Поле | Тип | Описание |
|------|-----|---------|
| stage_name | String | Стадия из выпадающего списка |
| visit_date | Date | Дата выезда |
| executor_name | String | ФИО исполнителя |
| notes | Text | Примечания |
| defects_found | Integer | Найдено дефектов |
| defects_resolved | Integer | Устранено дефектов |
| yandex_folder_path | String | Папка ЯД (disk:/...) |
| voice_url | String | Путь голосовой заметки на ЯД |

**Функционал:**
- Добавление/удаление выездов
- Итого по месяцам (`GET /{card_id}/visits/summary`)
- Кнопка «Файлы на ЯД» — прямой переход в папку выезда
- Экспорт отчётов: PDF и Excel
- Загрузка отчётных файлов в папку ЯД

## Голосовые заметки

**Поддерживается в:**
- История карточки надзора (SupervisionDetailPage)
- История карточки CRM (CrmCardPage)

**Технический стек:**
- `VoiceRecorder.vue` — браузерный MediaRecorder API
- Формат: webm/mp4 (зависит от браузера)
- Загрузка: `POST /api/files/upload` → сохраняется в папку проекта на ЯД
- `voice_url` — путь на ЯД, отображается кнопкой воспроизведения в истории
- Скрытие пути в истории (`[voice:path]` не показывается пользователю)

## Опросы клиентов (ClientSurvey)

**Интеграция с Яндекс Формами:**

| Endpoint | Назначение |
|----------|-----------|
| `POST /api/surveys/create` | Создать опрос для договора |
| `GET /api/surveys/contract/{id}` | Получить опрос по договору |
| `POST /api/surveys/{id}/resend` | Переотправить ссылку |
| `GET /api/surveys/stats` | Статистика по опросам |
| `POST /api/surveys/yandex-webhook` | Принять ответы (без авторизации) |
| `POST /api/surveys/manual-import` | Ручной импорт результатов |

**Поля опроса:**
- `survey_token` — уникальная ссылка для клиента
- `sent_at` / `completed_at` — когда отправлен/заполнен
- `responses` — JSON ответы из Яндекс Форм
- `overall_score` — средняя оценка (float)

## Уведомления

### Каналы уведомлений

| Канал | Настройка | Описание |
|-------|----------|---------|
| In-app | Всегда | Значок в шапке приложения |
| Telegram | Подписка | Бот присылает сообщение |
| Web Push | Подписка | Push в браузер (PWA) |

### Типы уведомлений

- Назначение исполнителем на стадию
- Сдача работы исполнителем
- Принятие/отклонение работы
- Отправка клиенту на согласование
- Согласование клиентом
- Просроченный дедлайн
- Перемещение карточки
- Новое сообщение в чате

### Web Push (VAPID)

- Сервис: `server/services/notification_service.py`
- Ключи: VAPID_PUBLIC_KEY + VAPID_PRIVATE_KEY в .env
- Подписка: `POST /api/notifications/subscribe`
- Отписка: `POST /api/notifications/unsubscribe`
- Настройки: `NotificationSettingsPage` (мобиль) + `NotificationSettingsWidget` (десктоп)

### WebSocket real-time

- Endpoint: `ws://host/ws/{token}`
- Обновление CRM доски при изменении карточек
- Мгновенные уведомления без polling

## Интеграция с Яндекс.Диском

### Структура папок

```
АРХИВ ПРОЕКТОВ/
  ФЕСТИВАЛЬ/
    Индивидуальные/
      СПБ/ | МСК/ | ВН/
    Шаблонные/
      СПБ/ | МСК/ | ВН/
    Авторские надзоры/
      СПБ/ | МСК/ | ВН/
  ПЕТРОВИЧ/
    (аналогичная структура)
```

### Подпапки договора

```
{agent}/{type}/{city}/{contract_number} {address}/
  ├── Документы/
  │   ├── Акты/
  │   └── Доп. соглашения/
  ├── Анкета/
  ├── Замер/
  ├── Стадия 1/
  ├── Стадия 2/
  └── Стадия 3/
```

### Автоматические операции

| Событие | Действие |
|---------|----------|
| Создание договора | Создаётся полная структура папок |
| Изменение типа/города/адреса | Папка перемещается (move-folder) |
| Удаление договора | Папка удаляется |
| Загрузка файла | Файл → Я.Диск → запись в project_files |
| Создание выезда | Создаётся папка выезда |

### URL формат

```python
yandex_folder_path = "disk:/CRM/Проекты/..."   # начинается с 'disk:'
# Для открытия в браузере:
path = yandex_folder_path.removeprefix("disk:")
url = f"https://disk.yandex.ru/client/disk{quote(path, safe='/')}"
```

### Ограничения

- Максимальный размер файла: **200 MB**
- Таймаут загрузки: `max(60, size_mb * 2 + 60)` секунд
- Retry: 3 попытки с backoff (1, 2, 4 сек)

## Offline режим

### Последовательность

```
1. Приложение пытается API запрос
2. При сетевой ошибке → fallback на локальную SQLite
3. Операции записи → очередь (offline_manager)
4. При восстановлении → синхронизация очереди
```

### Правило offline-очереди

**Только сетевые ошибки** попадают в очередь:
- `APIConnectionError` / `APITimeoutError`

**НЕ попадают в очередь:**
- Бизнес-ошибки (409 Conflict, 400 Bad Request)
- 403 Forbidden

### Поддерживаемые типы синхронизации

- client, contract, crm_card, supervision_card
- employee, payment, yandex_folder, project_file
- supervision_card pause/resume/reset_stage_completion

### Таймауты

| Параметр | Значение |
|----------|----------|
| DEFAULT_TIMEOUT | 10 сек |
| WRITE_TIMEOUT | 15 сек |
| OFFLINE_CACHE_DURATION | 10 сек |
| CONNECTION_CHECK | 60 сек |
| PING_TIMEOUT | 2 сек |

## Синхронизация при входе (utils/db_sync.py)

14 этапов полной синхронизации:
1. employees → 2. clients → 3. contracts
4. crm_cards → 5. supervision_cards
6. rates → 7. payments → 8. project_files
9. salaries → 10. stage_executors
11. approval_stage_deadlines → 12. action_history
13. supervision_project_history → 14. integrity_check

## Константы (config.py)

```python
APP_VERSION = "1.0.2"
PROJECT_TYPES = ['Индивидуальный', 'Шаблонный']
AGENTS = ['ФЕСТИВАЛЬ', 'ПЕТРОВИЧ']
CITIES = ['СПБ', 'МСК', 'ВН']
PAYMENT_TYPES = ['Аванс', 'Основной платеж', 'Оплата согласования']
```
