# План исправлений мобильной версии — Фаза 5

---

## 1. Глобальный поиск
### 1.1 Не закрывается после открытия
- **Причина:** Нет обработки Escape и клика вне диалога
- **Файл:** `mobile/src/layouts/MainLayout.vue` (строки 195-217)
- **Исправление:** Убрать `seamless` с q-dialog (это не позволяет закрыться по overlay), добавить кнопку закрытия

### 1.2 Не находит ничего
- **Причина:** Неправильный URL endpoint: вызывается `/api/v1/search/global`, а на сервере `/api/v1/search`
- **Файл:** `mobile/src/layouts/MainLayout.vue` (строка 291)
- **Исправление:** Изменить URL на `/api/v1/search`

---

## 2. Главная
### 2.1 На против заголовка уведомлений справа разместить кнопку: "прочитать все"
- **Файл:** `mobile/src/pages/DashboardPage.vue` (строка 70)
- **Исправление:** Рядом с `<q-badge>` добавить кнопку "Прочитать все" вызывающую `notificationsApi.markAllRead()`

---

## 3. Клиенты
### 3.1 Фильтр по типу не работает
- **Причина:** Функция `applyFilters()` пустая — не содержит логики фильтрации
- **Файл:** `mobile/src/pages/ClientsPage.vue` (строки 136-138)
- **Исправление:** Реализовать логику фильтрации по `client_type` (физ/юр лицо) через поле `organization_name`

### 3.2 Сортировка не срабатывает
- **Причина:** Функция `applyFilters()` пустая — сортировка не применяется
- **Файл:** `mobile/src/pages/ClientsPage.vue` (строки 136-138)
- **Исправление:** Реализовать сортировку по имени (alphabetical) и по дате (created_at desc)

---

## 4. Договора
### 4.1 Карточка на доске: Окрашивание по статусу
- **Файл:** `mobile/src/pages/ContractsPage.vue` (строки 143-148, функция `cardBgStyle`)
- **Текущее:** Только 2 цвета (СДАН → зелёный, В работе → жёлтый)
- **Исправление:** Добавить: авторский надзор → голубой (#E3F2FD), расторгнут → светло-красный (#FFEBEE), в работе → бледно-серый (#F5F5F5), новый заказ → белый

### 4.2 Внутри карточки: Геометка как в карточках СРМ
- **Файл:** `mobile/src/pages/ContractDetailPage.vue` (строки 10-28)
- **Текущее:** Уже есть кнопка location_on
- **Исправление:** Уже реализовано, при необходимости подправить стиль

### 4.3 Внутри карточки: Заголовок — фон бледно-серый
- **Файл:** `mobile/src/pages/ContractDetailPage.vue` (строки 4-28)
- **Исправление:** Добавить `style="background: #F5F5F5"` к q-card-section заголовка, шрифт сделать `color: #666`

### 4.4 Внутри карточки: Вертикальные разделения в заголовке
- **Файл:** `mobile/src/pages/ContractDetailPage.vue` (строки 22-26)
- **Текущее:** Разделитель ` · ` (точка)
- **Исправление:** Заменить ` · ` на `<span style="color: #ccc; margin: 0 4px">|</span>` между тип проекта | подтип проекта | площадь | город | этаж

### 4.5 Внутри карточки: Основные данные в 2 колонки
- **Файл:** `mobile/src/pages/ContractDetailPage.vue` (строки 42-73)
- **Текущее:** 1 колонка (q-list с q-item)
- **Исправление:** Заменить на `row q-col-gutter-md`:
  - Первая колонка (col-6): дата, срок
  - Вторая колонка (col-6): сумма, площадь, город
  - Комментарий — на всю ширину (col-12)
  - Колонки разделить серой вертикальной линией

### 4.6 Таблица сроков: пропущенные стадии отмечать "пропущено"
- **Файл:** `mobile/src/pages/ContractDetailPage.vue` (строки 127-150)
- **Исправление:** Добавить проверку на `status === 'skipped'` и отображать текст "Пропущено" с серой иконкой

### 4.7 Файлы: кнопки открыть и удалить покрупнее и в рамке
- **Файл:** `mobile/src/pages/ContractDetailPage.vue` (строки 152-189)
- **Исправление:** Заменить мелкие иконки на кнопки с outline рамкой, размер `size="sm"`

### 4.8 Все файлы: ограничить длину строк
- **Файл:** `mobile/src/pages/ContractDetailPage.vue`
- **Исправление:** Добавить `overflow: hidden; text-overflow: ellipsis; white-space: nowrap; max-width: calc(100% - 80px)` к именам файлов

### 4.9 Акты: разделить на 3 колонки
- **Файл:** `mobile/src/pages/ContractDetailPage.vue` (строки 191-227)
- **Текущее:** Кнопки загрузки уже в 3 колонках (col-4), но файлы в 1 колонку
- **Исправление:** Файлы тоже разместить в 3 колонки с кнопками действий. Для маленьких мониторов — 3 строки (responsive col-xs-12 col-sm-4)

### 4.10 Аналогично для актов с подписью
- **Исправление:** Применить ту же логику что и для актов без подписи

### 4.11 Голосовые сообщения загружаются в блок доп соглашения — неправильно
- **Файл:** `mobile/src/pages/ContractDetailPage.vue`
- **Исправление:** Проверить логику загрузки голосовых — если они попадают в `supplementary_agreements` группу, исправить на отдельную группу или правильную привязку

---

## 5. СРМ
### 5.1 Карточка на доске: Кнопка "переместить" должна пропускать подэтапы
- **Файл:** `mobile/src/pages/CrmBoardPage.vue` (строки 391-478)
- **Исправление:** При перемещении в следующую стадию, если в текущей есть незавершённые подэтапы — добавить подтверждение и пропуск подэтапов

### 5.2 Карточка на доске: Выровнять блок дедлайна
- **Файл:** `mobile/src/components/CrmCardItem.vue` (строки 60-63)
- **Исправление:** Добавить `width: 100%` или `align-self: stretch` к блоку дедлайна

### 5.3 Внутри карточки: Заголовок — фон бледно-серый
- **Файл:** `mobile/src/pages/CrmCardPage.vue` (строки 10-35)
- **Исправление:** Добавить `style="background: #F5F5F5"` к шапке карточки, шрифт `color: #666`

### 5.4 Внутри карточки: Блок подэтапа и "правка:1" выровнять по высоте и радиусам
- **Файл:** `mobile/src/pages/CrmCardPage.vue` (строки 29-34)
- **Исправление:** Унифицировать стили q-chip и q-badge — одинаковый border-radius (12px), одинаковая высота (24px)

### 5.5 Внутри карточки: Вертикальные разделения в заголовке
- **Файл:** `mobile/src/pages/CrmCardPage.vue` (строки 22-26)
- **Исправление:** Заменить ` · ` на `|` с разделителями

### 5.6 Внутри карточки: Блок "информация" в 2 колонки
- **Файл:** `mobile/src/pages/CrmCardPage.vue` (строки 65-76)
- **Исправление:**
  - Первая колонка: номер договора, тип проекта
  - Вторая колонка: срок договора, дата начала, дедлайн проекта
  - Разделить серой вертикальной линией

### 5.7 При обновлении страницы сохранять открытую вкладку
- **Файл:** `mobile/src/pages/CrmCardPage.vue`
- **Текущее:** Частично через `route.query.tab`
- **Исправление:** При смене вкладки обновлять URL query: `router.replace({ query: { ...route.query, tab: activeTab.value } })`

### 5.8 Вкладка "Сроки": Добавить "итого" в конце таблицы
- **Файл:** `mobile/src/pages/CrmCardPage.vue` (строки 160-197)
- **Исправление:** Добавить итоговую строку с суммой norm_days и actual_days

### 5.9 Вкладка "Сроки": Блок уведомления о перераспределении норма-дней
- **Файл:** `mobile/src/pages/CrmCardPage.vue`
- **Исправление:** Добавить предупреждающий блок (q-banner) при назначении исполнителя если norm_days были перераспределены

### 5.10 Данные: Добавить установку даты для ТЗ (для START)
- **Файл:** `mobile/src/pages/CrmCardPage.vue`
- **Исправление:** Добавить поле даты при загрузке ТЗ, синхронизировать с диалогом загрузки ТЗ в договоре

### 5.11 Кнопка вариации: не создаёт вкладку без файлов + не удаляет пустую
- **Файл:** `mobile/src/pages/CrmCardPage.vue` (строки 877-891)
- **Исправление:** При создании вариации — показывать вкладку сразу. При удалении — удалять без обновления страницы

### 5.12 В истории нет информации по загруженным файлам и дедлайнам
- **Файл:** `mobile/src/pages/CrmCardPage.vue` (строки 321-339)
- **Исправление:** Добавить отображение файлов и дедлайнов в лог действий

### 5.13 Архив: оформление, пропуск подэтапов, дата ТЗ, история, чат
- **Файл:** `mobile/src/pages/CrmCardPage.vue`
- **Исправление:** Применить стили активной карточки к архивной. Указывать пропуск подэтапов. Дата ТЗ. Чат только если был ранее создан.

---

## 6. СРМ надзора
### 6.1 Карточка на доске: тип агента и стадия
- **Файл:** `mobile/src/pages/SupervisionPage.vue` (строки 22-63)
- **Исправление:** Добавить badge с типом агента

### 6.2 Пауза/возобновить размещение
- **Файл:** `mobile/src/pages/SupervisionDetailPage.vue`
- **Исправление:** Переместить кнопки над "данными карточки", справа

### 6.3 При обновлении сохранять вкладку
- **Файл:** `mobile/src/pages/SupervisionDetailPage.vue`
- **Исправление:** Аналогично CRM — `router.replace` при смене вкладки

### 6.4 Заголовок оформить как в основной СРМ
- **Исправление:** Фон бледно-серый, разделители |

### 6.5 Кнопки действия по стилю как у основной СРМ
- **Исправление:** Заменить q-item на q-btn с цветными стилями

### 6.6 Вкладку "сроки" переименовать в "закупки"
- **Файл:** `mobile/src/pages/SupervisionDetailPage.vue` (строка 45)
- **Исправление:** `label="Закупки"`

### 6.7 Добавить отображение файлов в стадиях закупок
- **Текущее:** Файлы уже отображаются (строки 186-191)
- **Исправление:** Проверить публичные ссылки

### 6.8 В диалог закупок стадии добавить "комиссию"
- **Файл:** `mobile/src/pages/SupervisionDetailPage.vue`
- **Исправление:** Добавить поле commission в диалог editTimelineEntry

### 6.9 В блоке бюджет убрать дефекты, добавить комиссию
- **Файл:** `mobile/src/pages/SupervisionDetailPage.vue` (строки 223-247)
- **Исправление:** Убрать блок "Дефекты", добавить "Комиссия"

### 6.10 Отцентровать данные бюджета
- **Исправление:** `text-center` к блоку бюджета

### 6.11 Вкладку "данные" переименовать в "выезды"
- **Файл:** `mobile/src/pages/SupervisionDetailPage.vue`
- **Исправление:** `label="Выезды"`

### 6.12 Диалоговое окно и таблица выездов (2 типа: на объект и к поставщику)
- **Файл:** `mobile/src/pages/SupervisionDetailPage.vue`
- **Исправление:** Переработать блок выездов: тип выезда, план.дата, факт.дата, отчёт, фото, кнопки действия

### 6.13 Файлы закупок убрать — переезжают в закупки
- **Исправление:** Удалить блок "Файлы закупок" из вкладки "Данные"→"Выезды"

### 6.14 История: добавить фильтрацию
- **Исправление:** Добавить toggle фильтр по типам записей

### 6.15 Выезды в истории только фиксируем
- **Исправление:** Убрать добавление выездов из истории, оставить только отображение

### 6.16 Оплаты создаются после закрытия стадии
- **Исправление:** Проверить серверную логику

### 6.17 Заметки сделать как в основной СРМ
- **Исправление:** Унифицировать стиль с CrmCardPage

---

## 7. Отчёты и статистика перестали показывать данные
- **Файл:** `mobile/src/pages/ReportsPage.vue`
- **Причина:** Проверить API endpoints и параметры. Если запросы падают — данные молча не отображаются
- **Исправление:** Добавить обработку ошибок, проверить endpoints

---

## 8. Зарплаты
### 8.1 Статус при создании по умолчанию "в работе"
- **Файл:** `mobile/src/pages/SalariesPage.vue` (строки 506-530)
- **Исправление:** Добавить `payment_status: 'pending'` в payload функции createPayment

---

## 9. Уведомления
### 9.1 Вернуть кнопку "прочитать все"
- **Текущее:** Кнопка уже есть (строки 21-29)
- **Исправление:** Проверить видимость

### 9.2 Фильтрация: адрес, город, тип агента, тип уведомления
- **Текущее:** Есть только фильтр по типу уведомления
- **Исправление:** Добавить фильтры по адресу (text input), городу (select), типу агента (select)

---

## 10. Отчеты по сотрудникам
### 10.1 Имя и фамилия в сравнении
- **Файл:** `mobile/src/pages/EmployeeReportsPage.vue` (строки 39, 74)
- **Текущее:** В графиках используется `split(' ')[0]` — только первое слово
- **Исправление:** Использовать полное имя или первые 2 слова (Имя + Фамилия)
